import { defineComponent } from 'vue';

export const WelcomeOne = defineComponent({
  setup: (props, context) => {
    return () => (
      <div>WelcomeOne
      </div>
    )
  }
})

export default WelcomeOne